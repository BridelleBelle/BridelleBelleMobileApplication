<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:forms="clr-namespace:Xamarin.Forms;assembly=Xamarin.Forms.CarouselView"
             x:Class="BridelleBelleMobileApplication.PageView">
    <StackLayout>
    <Label Text="Carousel Images" 
           VerticalOptions="Center" 
           HorizontalOptions="Center" />

    <forms:CarouselView x:Name="MainCarouselView"
                        ItemSelected="MainCarouselView_ItemSelected">
        <forms:CarouselView.ItemTemplate>
            <DataTemplate>
                <Grid>
                    <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Image Grid.RowSpan="2" Aspect="AspectFill" Source="{Binding .}"/>
                    <StackLayout Grid.Row="1" BackgroundColor="#80000000" Padding="12">
                        <Label x:Name="MainLabel" TextColor="White" FontSize="16" HorizontalOptions="Center" VerticalOptions="CenterAndExpand"/>
                    </StackLayout>
                </Grid>
            </DataTemplate>
        </forms:CarouselView.ItemTemplate>
    </forms:CarouselView>

    </StackLayout>
</ContentPage>